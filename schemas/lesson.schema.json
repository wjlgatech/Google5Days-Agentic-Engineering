{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hub/schemas/lesson.schema.json",
  "title": "Lesson-Learned Entry",
  "description": "Turn a failure into a system improvement (Design Principle #8; Day 4/5 failure->golden-set). A lesson is incomplete until it names the concrete change that prevents recurrence.",
  "type": "object",
  "required": [
    "id",
    "date",
    "what_failed",
    "why_it_failed",
    "signal_missed",
    "system_improvement"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^L[0-9]{3,}$",
      "description": "e.g. L001"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "what_failed": {
      "type": "string"
    },
    "why_it_failed": {
      "type": "string",
      "description": "Root cause, not symptom."
    },
    "signal_missed": {
      "type": "string",
      "description": "What observable signal would have caught it earlier?"
    },
    "system_improvement": {
      "type": "object",
      "description": "The durable fix. At least one sub-field must be non-empty.",
      "additionalProperties": false,
      "properties": {
        "rule": {
          "type": "string"
        },
        "test_or_check": {
          "type": "string",
          "description": "Ideally a line added to scripts/check.sh or a new objective."
        },
        "prompt": {
          "type": "string"
        },
        "workflow": {
          "type": "string"
        },
        "tool": {
          "type": "string"
        },
        "guard": {
          "type": "object",
          "description": "Executable form of the fix: a shell cmd (exit 0 = the regression is still prevented). scripts/check.sh O14 runs it, so banking a lesson ratchets the gate up by one with zero edits to check.sh.",
          "additionalProperties": false,
          "properties": {
            "desc": {
              "type": "string"
            },
            "cmd": {
              "type": "string",
              "description": "Shell command; exits 0 if the lesson's fix holds."
            }
          }
        }
      }
    },
    "becomes_eval_case": {
      "type": "boolean",
      "description": "Day 4/5: was this added to the golden set / objective registry?"
    },
    "author": {
      "type": "string",
      "description": "Cohort attribution \u2014 who banked this lesson (org memory)."
    }
  }
}
